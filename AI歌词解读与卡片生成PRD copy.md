# AI歌词解读与卡片生成 — 产品需求文档（PRD）

---

## 1. 产品愿景与目标

**解决什么问题？**
听歌不仅仅是听旋律，更是在感受歌词背后的故事和情感。但大多数用户不会主动去搜索歌曲的创作背景，也没有合适的工具来记录和分享此刻的听歌心情。

**核心价值：**
- **深度理解**：AI自动解读歌曲背景和情感，让用户更懂音乐
- **情感表达**：结合歌词与心情生成精美心情卡片，让听歌时刻可分享
- **零门槛使用**：不需要用户额外搜索，播放器自动提供智能内容

---

## 2. 核心功能清单

### P0 — 核心功能（必须实现）

| # | 功能 | 描述 |
|---|------|------|
| P0-1 | **AI歌曲解读Tab** | 在右侧Tab区新增"AI解读"标签，展示歌曲创作背景故事 |
| P0-2 | **通义千问API集成** | 调用通义千问API，根据歌名和歌手生成歌曲背景介绍，曾经被经典影视作品引用（200-300字） |
| P0-3 | **歌词卡片生成器** | 用户可选择多个歌词片段+输入心情，生成简约文艺风卡片 |
| P0-4 | **卡片下载功能** | 点击下载按钮，保存PNG格式图片到本地 |

### P1 — 重要体验（显著提升满意度）

| # | 功能 | 描述 |
|---|------|------|
| P1-1 | **加载状态优化** | AI生成时显示优雅的加载动画和提示文案 |
| P1-2 | **错误处理** | API调用失败时友好提示，支持手动重试 |
| P1-3 | **歌词片段选择** | 在歌词列表中选中多行文本，自动填充到卡片生成器 |
| P1-4 | **API密钥配置** | 增加一个设置页面，平台管理员配置通义千问API Key，支持加密存储 |

### P2 — 锦上添花（后续迭代）

| # | 功能 | 描述 |
|---|------|------|
| P2-1 | **卡片历史记录** | 保存用户生成过的卡片，支持重新查看和下载 |
| P2-2 | **一键分享** | 支持直接分享到微信、微博等社交平台 |
| P2-3 | **情感分析增强** | AI分析歌词情感标签（如：治愈、伤感、励志） |

---

## 3. 用户流程与交互

### 3.1 AI歌曲解读流程

```
播放歌曲 → 点击"AI解读"Tab → 
自动加载解读内容（首次） → 展示创作背景故事 → 
可手动刷新重新生成
```

### 3.2 歌词卡片生成流程

```
点击"生成卡片"按钮 → 
在歌词中选择喜欢的片段（或手动输入） → 
输入此刻心情（一句话） → 
预览卡片效果 → 
点击"下载"保存到本地
```

### 3.3 关键交互细节

| 场景 | 行为 |
|------|------|
| AI解读加载 | 显示打字机效果，模拟AI正在生成内容 |
| 歌词选择 | 长按或框选歌词行，高亮显示已选中部分 |
| 心情输入 | 输入框限制50字以内，实时显示剩余字数 |
| 卡片预览 | 实时预览卡片效果，支持修改后重新生成 |
| 下载反馈 | 下载成功后短暂显示"已保存到下载文件夹"提示 |

### 3.4 页面布局

**整体布局说明：**
- 左侧：唱片区域（420px固定宽度）
- 右侧：歌曲信息 + Tab切换区域（占据剩余空间）
- Tab区域包括：「歌词 | 曲库 | 百科 | 相似推荐」
- **新增「AI解读」Tab，替换或补充「百科」Tab**

**AI解读Tab完整布局（右侧红框区域）：**
```
┌────────────────────────────────────────────────┐
│  上海一九四三                                    │
│  周杰伦 · 周杰伦精选集 · 本地                    │
├────────────────────────────────────────────────┤
│  [歌词] [曲库] [AI解读] [相似推荐]              │ ← Tab导航
├────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────────────────────────────┐     │
│  │  🎵 歌曲创作背景                      │     │
│  │  ─────────────────────────            │     │
│  │                                        │     │
│  │  《上海一九四三》创作于2003年，        │     │ ← AI生成内容
│  │  收录在专辑《叶惠美》中。这首歌...    │     │   (200-300字)
│  │  [AI生成的完整背景故事]               │     │
│  │                                        │     │
│  │  [🔄 重新生成]                         │     │
│  └──────────────────────────────────────┘     │
│                                                 │
│  ┌──────────────────────────────────────┐     │
│  │  💭 生成心情卡片                      │     │
│  │  ─────────────────────────            │     │
│  │                                        │     │
│  │  选择歌词片段：                        │     │
│  │  ┌────────────────────────────┐      │     │
│  │  │ 老唱盘皮箱装满了明信片的铁盒  │      │     │ ← 可输入或
│  │  │ 埋藏着一片玫瑰花瓣             │      │     │   从歌词选择
│  │  └────────────────────────────┘      │     │
│  │                                        │     │
│  │  此刻心情（50字以内）：              │     │
│  │  ┌────────────────────────────┐      │     │
│  │  │ 想起童年的夏天...              │ 32/50│     │
│  │  └────────────────────────────┘      │     │
│  │                                        │     │
│  │  [预览卡片] [下载PNG]                 │     │
│  └──────────────────────────────────────┘     │
│                                                 │
└────────────────────────────────────────────────┘
```

**歌词卡片样式（简约文艺风）：**
```
┌────────────────────┐
│                     │
│   渐变背景(科技绿)   │
│                     │
│   "你比从前快乐"     │
│   "在你眼中有光"     │
│                     │
│   ── 此刻心情 ──    │
│   "想起了老朋友"     │
│                     │
│   上海一九四三       │
│   周杰伦             │
│                     │
└────────────────────┘
```

---

## 4. 技术实现方案

### 4.1 通义千问API集成

**接口封装：**
- 创建 `/src/api/qwenApi.ts`
- 封装 `getSongBackground(artist: string, title: string)` 方法
- 使用通义千问 `qwen-turbo` 模型

**Prompt设计：**
```
请简要介绍歌曲《{title}》（{artist}演唱）的创作背景和故事，
包括创作时间、创作灵感、歌曲意义等。
要求：文字优美、简洁，200-300字以内，适合音乐爱好者阅读。
```

### 4.2 歌词卡片生成

**技术方案：**
- 使用 HTML5 Canvas API 绘制卡片
- 渐变背景：从 `#00ff88` 到 `#00cc6a` 的线性渐变
- 字体：苹方/思源黑体（中文），Arial（英文）
- 尺寸：1080x1920px（适合手机屏幕分享）

**Canvas绘制流程：**
1. 绘制渐变背景
2. 绘制歌词文本（居中，白色，大号字体）
3. 绘制分隔线
4. 绘制心情文本（居中，半透明白色，中号字体）
5. 绘制歌曲信息（底部，小号字体）
6. 转换为PNG格式并触发下载

### 4.3 组件结构

```
src/
├── api/
│   └── qwenApi.ts                    # 通义千问API封装
├── components/
│   ├── ContentTabs/
│   │   ├── TabContainer.tsx          # Tab容器（需修改，新增AI解读Tab）
│   │   ├── AIInsightTab.tsx          # 【新建】AI解读Tab主组件
│   │   └── LyricCardGenerator.tsx    # 【新建】歌词卡片生成器组件
│   └── Settings/
│       └── APISettings.tsx            # 【新建】API密钥配置组件
├── hooks/
│   └── useAIInsight.ts               # 【新建】AI解读数据管理Hook
├── utils/
│   └── cardCanvas.ts                 # 【新建】Canvas卡片绘制工具
└── store/
    └── settingsStore.ts               # 【新建】设置状态管理（存储API Key）
```

### 4.4 交互流程细化

**AI解读加载流程：**
1. 用户切换到"AI解读"Tab
2. 检查缓存中是否有该歌曲的解读内容
3. 如有缓存，直接展示；如无，显示加载动画
4. 调用通义千问API获取内容
5. 展示内容 + 缓存结果（避免重复调用）

**歌词选择交互：**
1. 在"歌词"Tab中，用户长按或框选歌词行
2. 选中的歌词行高亮显示（背景变色）
3. 出现"用于生成卡片"按钮
4. 点击后自动切换到"AI解读"Tab，歌词自动填入

**卡片生成流程：**
1. 用户填写歌词和心情
2. 点击"预览卡片"按钮
3. Canvas绘制卡片，实时预览
4. 用户满意后点击"下载PNG"
5. 浏览器触发下载，文件名为：`{歌名}-心情卡片-{时间戳}.png`

---

## 5. 验收标准

### P0 验收

| 验收项 | 通过标准 |
|--------|----------|
| AI解读生成 | 点击Tab后 ≤5s 内展示内容，内容流畅可读 |
| 歌词卡片生成 | 输入心情后 ≤2s 内生成预览，风格符合设计要求 |
| 卡片下载 | 点击下载后 ≤1s 保存成功，图片清晰（1080x1920） |
| API调用稳定性 | 失败率 ≤5%，失败后可重试 |

### P1 验收

| 验收项 | 通过标准 |
|--------|----------|
| 加载状态 | 显示友好的加载动画，不出现空白页面 |
| 错误提示 | API失败时清晰提示原因，提供"重试"按钮 |
| 歌词选择 | 支持选中1-5行歌词，选中后高亮显示 |

### 非功能性验收

| 维度 | 标准 |
|------|------|
| 性能 | AI解读内容缓存，同一首歌不重复调用API |
| 安全 | API Key加密存储在 localStorage，不暴露在代码中 |
| 体验 | 所有交互有明确的视觉反馈，按钮状态清晰 |
| 兼容性 | 在Chrome/Safari/Firefox最新版本正常运行 |

---

## 6. 开发优先级建议

**第一阶段（2-3天）：**
- P0-1: AI解读Tab基础框架
- P0-2: 通义千问API集成
- P0-3: 简单的歌词卡片生成（固定样式）

**第二阶段（1-2天）：**
- P0-4: 卡片下载功能
- P1-1: 加载状态优化
- P1-2: 错误处理完善

**第三阶段（1-2天）：**
- P1-3: 歌词片段选择交互
- P1-4: API密钥配置页面

**后续迭代：**
- P2功能根据用户反馈决定是否开发

---

## 7. 风险与限制

| 风险 | 应对方案 |
|------|----------|
| API费用超支 | 设置每日调用上限，提示用户配置自己的API Key |
| AI生成内容不准确 | 提供"重新生成"按钮，优化Prompt提示词 |
| 卡片样式单调 | 第一版先做简约风，后续根据反馈增加模板 |
| 歌词版权问题 | 卡片仅供个人使用，添加"仅供学习交流"水印 |

---

## 8. 成功指标

- **功能使用率**：≥30% 的用户点击过"AI解读"Tab
- **卡片生成率**：≥20% 的用户生成过歌词卡片
- **用户满意度**：AI生成内容准确度 ≥80%
- **分享传播**：≥10% 的用户下载卡片并分享到社交平台
